import { Create } from '@styled-icons/ionicons-solid/Create'
import * as Styled from './styles'

export const Pencil = () => {
  return (
    <Styled.Img data-testid='Pencil'>
      <Create />
    </Styled.Img>
  )
}