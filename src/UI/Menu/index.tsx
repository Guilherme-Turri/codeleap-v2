import { MenuOutline } from '@styled-icons/evaicons-outline/MenuOutline'
import * as Styled from './styles'

export const Menu = () => {
  return (
    <Styled.Img data-testid='MenuOutline'>
      <MenuOutline />
    </Styled.Img>
  )
}